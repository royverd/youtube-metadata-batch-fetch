# The whole corpus, not just the secret-bearing parts. Everything here is
# reproducible by re-running the pipeline, and a clone starting empty beats
# one carrying someone else's watchlist and API config.
data/
__pycache__/
*.egg-info/
build/
dist/
.venv/
t.txt
Untracked/
